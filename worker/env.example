# Zama Meme Battle Worker Environment Configuration

# Required - Contract address deployed on target network
CONTRACT_ADDRESS=0x1234567890abcdef1234567890abcdef12345678

# Required - Private key of the battle operator wallet
# This wallet must be set as the battleOperator in the contract
PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Optional - RPC endpoint (defaults to Sepolia)
RPC_URL=https://rpc.sepolia.org

# Optional - Cron schedule (defaults to every 30 seconds)
# Format: "second minute hour dayOfMonth month dayOfWeek"
# Examples:
#   "*/30 * * * * *"  - Every 30 seconds
#   "0 * * * * *"     - Every minute
#   "0 */5 * * * *"   - Every 5 minutes
CRON_SCHEDULE=*/30 * * * * *
